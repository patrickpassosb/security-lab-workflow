schema: security-lab/eval-case/v1
case_id: discovery-bizlogic-state-transition-001
suite: discovery-v1
split: train
description: "Business-logic / state-transition (CWE-840): checkout confirm does not enforce step ordering or revoked-payment-method re-check"
category: business-logic
tags: [business-logic, state-transition, workflow, cwe-840, discovery]
environment:
  target: shop.synth-test.example
  target_kind: synthetic
  max_target_requests: 0
authorization:
  actors:
    - {id: USR_BUYER_00000001, role: buyer}
  state_machine:
    intended: ["cart", "address_selected", "payment_selected", "confirmed"]
  invariants:
    - "confirm requires state=payment_selected"
    - "confirm rejects revoked payment methods"
# No expected answers here - those are in private/labels.json (gitignored)