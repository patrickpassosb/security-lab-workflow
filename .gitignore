# ─── Third-party content (own licenses; users clone themselves) ───
sandboxes/
tools/
wordlists/
proxy/
containers/

# ─── Engagement-specific workspaces (YOUR data, never committed) ───
ctfs/
bounties/
cves/
findings/
gbrain/learning/

# ─── Real engagement scope files (private — never commit real target scope) ───
# Synthetic example-* engagement files ARE committable (see allowlist below).
engagements/bounty-notion.yaml
engagements/bounty-*.yaml
!engagements/example-bounty.yaml
!engagements/example-ctf.yaml

# ─── Self-improvement private state (SI-000) ─────────────────────────────────
# Private evaluation labels/holdouts, candidate staging, runtime state.
# Tracked policy/config under improvement/policy/ and improvement/config/
# remains committable (split formalized in SI-002).
evals/**/private/
evals/**/expected/
improvement/state/
improvement/runs/
improvement/candidates/
improvement/private/

# ─── Personal artifacts at repo root ───
.audit.jsonl
PLAN.md
PLAN_OPEN_SOURCE.md
PLAN_OPEN_SOURCE.html
PRE_STAGE_REPORT.md
POST_CTF_PLAN.md
docs/open-source-plan.html
*-lessons.md
*-postmortem.md
*-handoff.md
fastapi-*.md
pycel-*.md
MULTI_ENGAGEMENT_PLAN.html
/ctf/
/ctf-fluid-attacks/

# ─── Stale backups ───
*.bak.*
scope.yaml.bak.*

# ─── Local install artifacts ───
.agents/

# ─── OS / editor cruft ───
.DS_Store
*.swp
*.swo
*~
.venv/
node_modules/
__pycache__/
*.pyc

# ─── CI / test artifacts (CI modernization) ───────────────────────────────
# Generated by `make test` and the CI pytest job; uploaded as workflow
# artifacts, never committed to the repo.
test-results.xml
coverage.xml
.coverage
.pytest_cache/
.ruff_cache/
.mypy_cache/

# ─── Secrets (the .env pattern) ───
.env
.env.*
!.env.example

# ─── Keys, certs, credentials (T2-27) ───
*.pem
*.key
*.crt
*.p12
*.pfx
*.keystore
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub
id_ecdsa
id_dsa
known_hosts
authorized_keys

# ─── Databases and local state ───
*.sqlite
*.sqlite3
*.db
*.db-journal
brain.pglite/
.aws/
.gnupg/
.ssh/

# ─── Evidence dumps (defensive: even inside tracked dirs) ───
core.*
*.core